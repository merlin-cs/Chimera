(declare-fun a () String)
(declare-fun b () String)
(declare-const c String)
(declare-const d String)
(declare-fun g () Bool)
(declare-fun e () Bool)
(declare-fun f () String)
(assert (str.in.re (str.substr a 0 (str.len (str.substr a 0 (str.len (str.substr a 0 (str.len c)))))) (re.* (re.++ (str.to.re "") (re.* (str.to.re "aa"))))))
(assert (str.in.re (str.substr b 0 (str.len d)) (re.* (re.++ (re.* (str.to.re "")) (str.to.re "bb")))))
(assert (= 4 (str.len d)))
(assert (not (= (str.substr a 0 (str.len c)) d)))
(assert (= g (not (= "file" (str.substr a (str.len c) (str.len f))))))
(assert (= e (not g)))
(assert e)
(check-sat)
